function parChi()
{
    const findfruit = document.querySelector("#fruitTable");
    var inarray = findbooks.list;
    
        Array.from(inarray).forEach(function(ins){
        ins.innerHTML = ins.textContent.toUpperCase();
        })

}